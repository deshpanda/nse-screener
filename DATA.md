# Data availability

Small derived datasets are committed under data/ (see below). Large raw
panels (bhav/, futstk/, ann_full/, pit/, fr_xbrl/, deals_hist/, mto/) are
NOT in git — hundreds of MB, fully reproducible via the ingest/ modules
(each file's header documents its source + quirks). Zipped snapshots may
be attached to GitHub Releases later.

Committed: corporate_actions.parquet, etf_list.parquet, sector_map.parquet,
india_vix.parquet, symbolchange.parquet, implied_splits.csv, fr_list.parquet,
index_members/.
